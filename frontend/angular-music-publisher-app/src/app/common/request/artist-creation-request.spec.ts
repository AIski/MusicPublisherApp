import { ArtistCreationRequest } from './artist-creation-request';

describe('ArtistCreationRequest', () => {
  it('should create an instance', () => {
    expect(new ArtistCreationRequest()).toBeTruthy();
  });
});
