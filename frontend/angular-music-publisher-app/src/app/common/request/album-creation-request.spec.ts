import { AlbumCreationRequest } from './album-creation-request';

describe('AlbumCreationRequest', () => {
  it('should create an instance', () => {
    expect(new AlbumCreationRequest()).toBeTruthy();
  });
});
