import { SongCreationRequest } from './song-creation-request';

describe('SongCreationRequest', () => {
  it('should create an instance', () => {
    expect(new SongCreationRequest()).toBeTruthy();
  });
});
